import type { NextPage } from "next";

// Import components
import MintPage from "../../components/ListingPage/ListingPage";

const Home: NextPage = () => {
    return <MintPage />;
};

export default Home;
